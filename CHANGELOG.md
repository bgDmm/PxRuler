# 更新日志

本项目的所有重要变更都会记录在此文件。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

---

## 📋 插件市场更新说明（v1.2.1）

> 提交 uTools 插件更新时可直接复制以下内容：

```
1. 修复关闭取色器后，鼠标移入标尺不显示十字光标、右键失灵的问题
2. 刻度数字新增描边效果，任意背景色下都清晰可读
3. 前景色（刻度、数字、实时数值）现在会根据背景明暗自动切换深浅配色
4. 移除每像素刻度线，改为 5/10/50/100 四级刻度，观感更干净
```

---

## [1.2.1] - 2026-09-04

### 修复

- **关闭取色器后鼠标穿透状态卡死** — 修改背景色后，鼠标移入标尺大概率显示为箭头而非十字光标，且右键无法再次唤出菜单，必须反复移入移出才能恢复
  - 根因一：`isOnRuler` 标志位未同步。取色器居中弹出、正好盖住标尺，关闭时指针从未移出过，`over && !isOnRuler` 恒为假，再也不会补发 `mouse-over`
  - 根因二：宿主在 `color-picker-hide` 时无条件立即恢复穿透，而 Electron 在 `setIgnoreMouseEvents` 切换瞬间会吞掉紧随其后的鼠标移动，导致指针移向标尺的那一次移动丢失
  - 修复：renderer 端记录 `lastMousePos`，`closeColorPicker()` 重置 `isOnRuler` 并按最后已知指针位置重新判定；宿主端新增 `pointerOnRuler` 跟踪，穿透恢复延后 300ms 且回调内跳过仍悬停的情况
  - 涉及文件：`ruler.js`、`preload.js`

## [1.2.0] - 2026-09-04

### 新增

- **前景色随背景亮度自动切换** — 刻度线、百位数字、数字描边、hover 实时数值、拖拽手柄会根据背景明暗自动选择深浅配色
  - 采用 WCAG 相对亮度公式（`0.2126R + 0.7152G + 0.0722B`，sRGB 线性化）
  - 渐变背景取起止两端中点色计算，避免落在渐变任一端时判断失准
  - 背景 alpha < 1 时按 alpha 与假定的浅色屏幕底色（245）混合后再计算，贴合透明浮窗的实际观感
  - 切换阈值 0.18（WCAG 黑白对比度标准中点）
  - hover 定位竖线固定为 `#667eea`，深浅背景下均保持醒目

### 修复

- **刻度上的半透明横线** — 原本对每个像素都绘制 0.5px 宽、4px 高的细线，密集细线在高分屏缩放下糊成一条灰带压在刻度上
  - 移除每像素刻度，仅保留 5 / 10 / 50 / 100 四级刻度与百位数字
- **文字在浅色背景下难以辨认** — 为四处文字（横向 / 纵向百位标签、hover 实时数值 ×2）增加描边
  - 先 `strokeText` 再 `fillText`，描边 3px、圆角连接
  - 深背景用 `rgba(0,0,0,0.7)` 深色描边，浅背景用 `rgba(255,255,255,0.85)` 浅色描边

## [1.1.0] - 2026-07-02

### 新增

- 标尺 UI 与样式全面重构（`ruler.css` 大幅扩充）
- 核心绘制与交互逻辑重写（`ruler.js`）
- 宿主端窗口管理增强（`preload.js`、`ruler_preload.js`）

## [1.0.0] - 2026-07-02

### 新增

- 首个可用版本
- Canvas 渲染的像素标尺，支持横向 / 纵向切换
- 鼠标悬停实时显示像素位置
- 拖拽移动、边缘拖拽调整长度
- 右键菜单：自定义背景色（HSV 取色器 + 透明度）、关闭标尺
- 双击右键快速关闭
- 状态持久化（方向、长度、背景色）

---

[1.2.1]: https://github.com/bgDmm/PxRuler/releases/tag/v1.2.1
[1.2.0]: https://github.com/bgDmm/PxRuler/releases/tag/v1.2.0
[1.1.0]: https://github.com/bgDmm/PxRuler/releases/tag/v1.1.0
[1.0.0]: https://github.com/bgDmm/PxRuler/releases/tag/v1.0.0
